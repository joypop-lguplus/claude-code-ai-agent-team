# 03 — API 사양

> `01-requirements.md`로부터 `/claude-sdd:sdd-spec`이 자동 생성

## 기본 URL

`{{BASE_URL}}`

## 인증

{{AUTH_METHOD_DESCRIPTION}}

## 엔드포인트

### {{ENDPOINT_GROUP}}

#### {{METHOD}} {{PATH}}

- **설명**: {{DESCRIPTION}}
- **인증**: {{AUTH_REQUIRED}}
- **스펙 ID**: {{SPEC_ID}}

**요청**:

```json
{{REQUEST_SCHEMA}}
```

| 필드 | 타입 | 필수 | 유효성 검증 | 설명 |
|------|------|------|-------------|------|
| {{FIELD}} | {{TYPE}} | {{REQUIRED}} | {{VALIDATION}} | {{DESCRIPTION}} |

**응답** ({{STATUS_CODE}}):

```json
{{RESPONSE_SCHEMA}}
```

**에러 응답**:

| 상태 코드 | 에러 코드 | 설명 |
|-----------|-----------|------|
| 400 | {{ERROR_CODE}} | {{ERROR_DESCRIPTION}} |
| 422 | {{ERROR_CODE}} | {{ERROR_DESCRIPTION}} |

---

## 공통 응답 형식

```json
{
  "success": true,
  "data": {},
  "error": null,
  "meta": {
    "page": 1,
    "limit": 20,
    "total": 100
  }
}
```

## 페이지네이션

{{PAGINATION_STRATEGY}}

## 요청 제한

{{RATE_LIMIT_DESCRIPTION}}

{{#if LEGACY}}
## 변경 태그

> 각 엔드포인트에 다음 태그를 표시합니다: `UNCHANGED` | `MODIFY` | `NEW` | `DEPRECATE`

### 변경된 엔드포인트

#### {{METHOD}} {{PATH}} — `MODIFY`

- **변경 사유**: {{CHANGE_REASON}}
- **원본**: #{{SECTION_REF}}

**변경 전**: {{BEFORE_DESCRIPTION}}
**변경 후**: {{AFTER_DESCRIPTION}}

### 신규 엔드포인트

#### {{METHOD}} {{PATH}} — `NEW`

- **추가 사유**: {{ADDITION_REASON}}

### 폐기 엔드포인트

| 엔드포인트 | 태그 | 사유 | 대안 |
|------------|------|------|------|
| {{METHOD}} {{PATH}} | `DEPRECATE` | {{REASON}} | {{ALTERNATIVE}} |

### 하위 호환성 계획

{{BACKWARD_COMPATIBILITY_PLAN}}
{{/if}}

---

*스펙 ID: API | 생성 원본: 01-requirements.md*
