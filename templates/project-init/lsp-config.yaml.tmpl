# LSP 설정 — sdd-config.yaml에 추가
# boostvolt/claude-code-lsps 플러그인으로 LSP를 Claude Code 내장 기능으로 활용
# 설치: claude plugin marketplace add boostvolt/claude-code-lsps
#        claude plugin install <플러그인명>@claude-code-lsps

lsp:
  # LSP 통합 활성화 여부 (true/false)
  enabled: true

  # 사용할 언어 서버 (auto = 프로젝트 언어에 따라 자동 선택)
  server: auto

  # 진단 결과 대기 시간 (밀리초)
  diagnostics_wait_ms: 3000

  # 언어별 서버 오버라이드 (선택 사항)
  # boostvolt/claude-code-lsps 플러그인 사용 시 자동 설정되므로 일반적으로 불필요
  # servers:
  #   typescript:
  #     command: vtsls
  #     args: ["--stdio"]
  #   python:
  #     command: pyright
  #     args: ["--stdio"]
  #   go:
  #     command: gopls
  #     args: ["serve"]
  #   java:
  #     command: jdtls
  #     args: []
  #   kotlin:
  #     command: kotlin-lsp
  #     args: []
